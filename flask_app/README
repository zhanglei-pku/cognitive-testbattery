BEFORE running the code, you need to add a symlink to handle static files. Run that command:

ln -s js $(pwd)/js flask_app/static/js
ln -s $(pwd)/css flask_app/static/css
ln -s $(pwd)/font flask_app/static/font
ln -s $(pwd)/img flask_app/static/img

To run the flask-app:

cd flask_app
python3 -m flask --app flask_app run
